{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Launch",
            "request": "launch",
            "type": "lldb",
            "preLaunchTask": "Build",
            "cwd": ".",
            "program": "${workspaceRoot}/build/out",
        }
    ]
}
